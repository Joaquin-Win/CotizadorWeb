// TeamSwitcher eliminado — ya no se usa el sistema Teams.
// Este stub existe para no romper imports mientras se limpia el código.
export function TeamSwitcher(_props: { inHeader?: boolean }) {
    return null;
}
